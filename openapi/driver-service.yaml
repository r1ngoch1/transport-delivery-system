openapi: 3.0.3
info:
  title: Driver Service API
  version: 1.0.0
servers:
  - url: http://localhost:8086
security:
  - bearerAuth: []
paths:
  /api/drivers/me:
    get:
      tags: [Drivers]
      summary: Get current driver profile
      operationId: getCurrentDriverProfile
      responses:
        '200':
          description: Current driver profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DriverProfile'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [Drivers]
      summary: Create current driver profile
      operationId: createCurrentDriverProfile
      parameters:
        - $ref: '#/components/parameters/UserIdHeader'
        - $ref: '#/components/parameters/RolesHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCurrentDriverProfileRequest'
      responses:
        '201':
          description: Created driver profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DriverProfile'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/Conflict'
    patch:
      tags: [Drivers]
      summary: Update current driver profile
      operationId: updateCurrentDriverProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDriverProfileRequest'
      responses:
        '200':
          description: Updated driver profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DriverProfile'
  /api/drivers/me/availability:
    get:
      tags: [Drivers]
      summary: List current driver availability slots
      operationId: listCurrentDriverAvailabilitySlots
      parameters:
        - $ref: '#/components/parameters/UserIdHeader'
      responses:
        '200':
          description: Current driver availability slots
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DriverAvailabilitySlot'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [Drivers]
      summary: Create current driver availability slot
      operationId: createCurrentDriverAvailabilitySlot
      parameters:
        - $ref: '#/components/parameters/UserIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DriverAvailabilitySlotRequest'
      responses:
        '201':
          description: Created availability slot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DriverAvailabilitySlot'
        '400':
          $ref: '#/components/responses/BadRequest'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/drivers/me/availability/{id}:
    patch:
      tags: [Drivers]
      summary: Update current driver availability slot
      operationId: updateCurrentDriverAvailabilitySlot
      parameters:
        - $ref: '#/components/parameters/UserIdHeader'
        - $ref: '#/components/parameters/AvailabilitySlotId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DriverAvailabilitySlotRequest'
      responses:
        '200':
          description: Updated availability slot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DriverAvailabilitySlot'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
    delete:
      tags: [Drivers]
      summary: Delete current driver availability slot
      operationId: deleteCurrentDriverAvailabilitySlot
      parameters:
        - $ref: '#/components/parameters/UserIdHeader'
        - $ref: '#/components/parameters/AvailabilitySlotId'
      responses:
        '204':
          description: Availability slot deleted
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /api/drivers:
    get:
      tags: [Drivers]
      summary: List driver profiles
      operationId: listDriverProfiles
      responses:
        '200':
          description: Driver profiles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DriverProfile'
    post:
      tags: [Drivers]
      summary: Create driver profile
      operationId: createDriverProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDriverProfileRequest'
      responses:
        '201':
          description: Created driver profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DriverProfile'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/drivers/available:
    get:
      tags: [Drivers]
      summary: List active available driver profiles
      operationId: listAvailableDriverProfiles
      responses:
        '200':
          description: Available driver profiles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DriverProfile'
  /api/drivers/{id}:
    get:
      tags: [Drivers]
      summary: Get driver profile by id
      operationId: getDriverProfile
      parameters:
        - $ref: '#/components/parameters/DriverId'
      responses:
        '200':
          description: Driver profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DriverProfile'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [Drivers]
      summary: Update driver profile by id
      operationId: updateDriverProfile
      parameters:
        - $ref: '#/components/parameters/DriverId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminUpdateDriverProfileRequest'
      responses:
        '200':
          description: Updated driver profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DriverProfile'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    UserIdHeader:
      name: X-User-Id
      in: header
      required: true
      schema:
        type: string
        format: uuid
    RolesHeader:
      name: X-User-Roles
      in: header
      required: false
      schema:
        type: string
    DriverId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    AvailabilitySlotId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    BadRequest:
      description: Request validation failed
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Conflict:
      description: Request conflicts with current state
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Forbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
  schemas:
    DriverAvailabilityStatus:
      type: string
      enum: [AVAILABLE, UNAVAILABLE, ON_TRIP, SUSPENDED]
    DriverProfile:
      type: object
      required: [id, userId, fullName, phone, licenseNumber, licenseCategory, licenseExpiresAt, availabilityStatus, active, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        fullName:
          type: string
        phone:
          type: string
        licenseNumber:
          type: string
        licenseCategory:
          type: string
        licenseExpiresAt:
          type: string
          format: date
        availabilityStatus:
          $ref: '#/components/schemas/DriverAvailabilityStatus'
        active:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    DriverAvailabilitySlot:
      type: object
      required: [id, driverProfileId, startAt, endAt, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        driverProfileId:
          type: string
          format: uuid
        startAt:
          type: string
          format: date-time
        endAt:
          type: string
          format: date-time
        note:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    DriverAvailabilitySlotRequest:
      type: object
      required: [startAt, endAt]
      properties:
        startAt:
          type: string
          format: date-time
        endAt:
          type: string
          format: date-time
        note:
          type: string
    CreateDriverProfileRequest:
      type: object
      required: [userId, fullName, phone, licenseNumber, licenseCategory, licenseExpiresAt]
      properties:
        userId:
          type: string
          format: uuid
        fullName:
          type: string
        phone:
          type: string
        licenseNumber:
          type: string
        licenseCategory:
          type: string
        licenseExpiresAt:
          type: string
          format: date
        availabilityStatus:
          $ref: '#/components/schemas/DriverAvailabilityStatus'
        active:
          type: boolean
    CreateCurrentDriverProfileRequest:
      type: object
      required: [fullName, phone, licenseNumber, licenseCategory, licenseExpiresAt]
      properties:
        fullName:
          type: string
        phone:
          type: string
        licenseNumber:
          type: string
        licenseCategory:
          type: string
        licenseExpiresAt:
          type: string
          format: date
    UpdateDriverProfileRequest:
      type: object
      properties:
        phone:
          type: string
        licenseNumber:
          type: string
        licenseCategory:
          type: string
        licenseExpiresAt:
          type: string
          format: date
        availabilityStatus:
          $ref: '#/components/schemas/DriverAvailabilityStatus'
    AdminUpdateDriverProfileRequest:
      allOf:
        - $ref: '#/components/schemas/UpdateDriverProfileRequest'
        - type: object
          properties:
            fullName:
              type: string
            active:
              type: boolean
    ErrorResponse:
      type: object
      required: [timestamp, status, error, message, path]
      properties:
        timestamp:
          type: string
          format: date-time
        status:
          type: integer
        error:
          type: string
        message:
          type: string
        path:
          type: string
