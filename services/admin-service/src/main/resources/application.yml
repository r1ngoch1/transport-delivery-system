server:
  port: 8089

spring:
  application:
    name: admin-service
  config:
    import: optional:configserver:http://localhost:8889

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
