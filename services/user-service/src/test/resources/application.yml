spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:1/test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

app:
  jwt:
    secret: "system-transport-test-secret-must-be-at-least-32-bytes"
